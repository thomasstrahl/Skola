This package contains a skeleton for classes to represent an expression in predicate logic. 

 An expr is either ...
   
 The interface Expr specifies one methods. It has to be implemented properly.
 
 There two classes to test the Expr classes. You may remove one of them. Initially the classes will not compile 
 since some classes are missing.
 
   SimplePredicateTest that just prints expected and calculated values.
   
   TestPredicate that uses the JUnit framework by extending TestTerm. 